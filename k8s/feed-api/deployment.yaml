apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: gcr.io/studious-loader-278016/users-service-api:0.0.9
          ports:
            - containerPort: 7070
          envFrom:
            - configMapRef:
                name: api-config
          readinessProbe:
            httpGet:
              path: /
              port: 7070
          resources:
            requests:
              cpu: 200m
              memory: 300Mi
            limits:
              cpu: 500m
              memory: 400Mi
